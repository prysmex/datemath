name: Test

on: [pull_request]

# permissions:
#   contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    # env:
    #   BUNDLE_ONLY: rubocop

    steps:
    - uses: actions/checkout@v4

    - name: Set up Ruby 4.0.6
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 4.0.6
        bundler-cache: true

    - name: Run Tests
      run: bundle exec rake
      # run: bundle exec rubocop --parallel